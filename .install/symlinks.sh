#!/bin/bash

echo -e "${GREEN}"
cat <<"EOF"

█▀ █▄█ █▀▄▀█ █░░ █ █▄░█ █▄▀ █▀
▄█ ░█░ █░▀░█ █▄▄ █ █░▀█ █░█ ▄█

EOF
echo -e "${NONE}"
if [ -f "${HOME}/.bashrc" ]; then
    
fi

# ln -sf "$DOTFILES/hypr" "$CONFIG/hypr" && echo "> Created symlink for hypr"
# ln -sf "$DOTFILES/waybar" "$CONFIG/waybar" && echo "> Created symlink for waybar"
# ln -sf "$DOTFILES/alacritty" "$CONFIG/alacritty" && echo "> Created symlink for alacritty"
# ln -sf "$DOTFILES/neofetch" "$CONFIG/neofetch" && echo "> Created symlink for neofetch"
# ln -sf "$DOTFILES/nvim" "$CONFIG/nvim" && echo "> Created symlink for nvim"
# ln -sf "$DOTFILES/starship/starship.toml" "$CONFIG/starship.toml" && echo "> Created symlink for starship.toml"
# ln -sf "$DOTFILES/ranger" "$CONFIG/ranger" && echo "> Created symlink for ranger"
# ln -sf "$DOTFILES/wofi" "$CONFIG/wofi" && echo "> Created symlink for wofi"
# ln -sf "$DOTFILES/zathura" "$CONFIG/zathura" && echo "> Created symlink for zathura"